# This project's own trust store override. The machine-global ~/.npmrc points
# cafile at the Cisco Secure Access root CA, which is not in the chain presented
# by registry.npmjs.org (public Google Trust Services certs), so npm fails TLS
# verification. The macOS system bundle validates everything we need.
cafile=/etc/ssl/cert.pem
strict-ssl=true